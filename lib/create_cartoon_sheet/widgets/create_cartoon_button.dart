import 'package:flutter/material.dart';

class CreateCartoonButton extends StatelessWidget {
  const CreateCartoonButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
